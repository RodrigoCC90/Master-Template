name: orbitos_mobile
description: "OrbitOS Mobile - Operations Management Platform"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Microsoft Authentication Library
  msal_flutter: ^2.0.1

  # HTTP client
  http: ^1.2.0

  # Secure storage for tokens
  flutter_secure_storage: ^9.2.0

  # State management
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
